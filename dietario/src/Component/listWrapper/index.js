import React from 'react';
import {Typography, Container,Grid} from '@material-ui/core';

const ListWrapper = () =>{
    return (
      <>
        <Typography>this is a list Wrapper</Typography>
      </>
    );
}

export default ListWrapper;